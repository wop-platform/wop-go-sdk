# Mutation kill-rate 证据 — wop-go-sdk 初证 2026-08-29

> 文件名沿用工具链 `run.py write_stamp()` 默认锚（stamp 的 evidence 字段指向本文件名）；
> 本仓初证日期见标题（移植自 gtsp-wop-gateway 黑盒工厂分发，defects.json 已本地化）。

- 命令：`python3 .factory/mutations/run.py`（全量，无 --only 过滤）
- 退出码：**0**（全部按预期；无 FAIL / SKIP / 配置错误；还原零残留——runner 内置逐文件字节校验通过）
- 缺陷集：13 条 = 篡改类 6（guard 门）+ 行为破坏类 5（tests 门）+ 负例 2（guard/tests 各 1）
- tests 门：`scripts/run_tests.sh --no-lock` = go vet + `go test ./... -coverpkg=.` + 覆盖率门禁 98%（基线 99.1%）

## 汇总（原文摘录）

```
===== mutation 冒烟汇总 =====
  [G-01] PASS       blocked=True（rc=1）符合预期
  [G-02] PASS       blocked=True（rc=1）符合预期
  [G-03] PASS       blocked=True（rc=2）符合预期
  [G-04] PASS       blocked=True（rc=1）符合预期
  [M-01] PASS       blocked=True（rc=2）符合预期
  [M-02] PASS       blocked=True（rc=2）符合预期
  [B-01] PASS       blocked=False（rc=0）符合预期
  [B-101] PASS      blocked=True（rc=1）符合预期
  [B-102] PASS      blocked=True（rc=1）符合预期
  [B-103] PASS      blocked=True（rc=1）符合预期
  [B-104] PASS      blocked=True（rc=1）符合预期
  [B-105] PASS      blocked=True（rc=1）符合预期
  [B-106] PASS      blocked=False（rc=0）符合预期
  篡改类拦截（guard 门）: 6/6 = 100%
  行为破坏类拦截（tests 门）: 5/5 = 100%
  正向缺陷拦截（kill rate）: 11/11 = 100%
  负例放行: 2/2
  结论: 门灵敏度冒烟通过（auto-merge 的必要非充分条件）
  周界指纹已绑定: 257225231286（evidence-stamp.json）
```

## 击杀明细（tests 门 → 首个红测锚）

| 缺陷 | 注入点 | 击杀证据（被拦截的断言面） |
|---|---|---|
| B-101 | digest.go `sha256.Sum256(data)` → `(nil)` | TestDigest_VectorByteLevel / vectors_conformance 字节级摘要 |
| B-102 | canonical.go `if i > 0 {` → `if false {` | go vet + TestCanonicalHeaders 多标头 '\n' 分隔 |
| B-103 | signature.go RSA 验签 `err != nil` → `err == nil` | TestSignMessage_RSAVectors_ByteLevel / TestVerifyResponse 正负路径 |
| B-104 | keyencrypt.go OAEP `payload` → `nil` | TestWrapUnwrapDEK_Roundtrip / vectors roundtrip 字节等值 |
| B-105 | verify.go DEK 解包 `&c.merchantPriv` → `&privKey{}` | TestVerifyResponse_L0_L2_Happy / EndToEnd L2 |

## 环境与前置

- 仓库：wop-platform/wop-go-sdk（main @ e8655d5）
- 门基线：`scripts/run_tests.sh --no-lock` 干净树 rc=0（vet 0 finding，total=99.1% ≥ 98%）
- guard 自检：`PERIMETER ↔ MISSION.md 周界清单` 19 条一致，路径存在性通过
- 周界指纹绑定：evidence-stamp.json `perimeter_blob` = 本仓 factory-local.json 的 git blob（257225231286…）
- auto-merge：本仓默认关闭（铁律 5：本证明是必要非充分条件，开启与否由人类决定）
